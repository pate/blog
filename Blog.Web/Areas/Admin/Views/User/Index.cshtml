@model MembershipUserCollection

@{
    ViewBag.PageTitle = "Index";
}

<div class="page-header">
    <h1>Users</h1>
</div>

@foreach (MembershipUser user in Model)
{
    <a href="@Url.Action("Edit", new { id = user.UserName })">Edit User</a>
    @Html.DisplayFor(x => user)
}