@model PagedList.IPagedList<Blog.Data.Models.Page>

@{
    ViewBag.PageTitle = "Index";
}

<div class="page-header">
    <h1>Pages</h1>
</div>

@*<div class="navbar">
    <div class="navbar-inner">
        <a class="brand" href="#">Title</a>
        <ul class="nav">
            <li class="active"><a href="#">Create Page</a></li>
            <li><a href="#">Link</a></li>
            <li><a href="#">Link</a></li>
        </ul>
    </div>
</div>*@
<div class="navbar">
    <div class="navbar-inner">
        <ul class="nav nav-pills">
            <li><a href="@Url.Action("Create")">Create Page</a></li>
            <li><a href="#">Link</a></li>
            <li><a href="#">Link</a></li>
        </ul>
    </div>
</div>

@if (Model.Any())
{
    <ul>
        @foreach (var page in Model)
        {
            <li><a href="@Url.Action("Details", "Page", new {id = page.Id})">@page.Title</a></li>
        }
    </ul>
}
else
{
    <p>No pages.</p>
}
